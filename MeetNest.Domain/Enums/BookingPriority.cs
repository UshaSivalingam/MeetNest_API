namespace MeetNest.Domain.Enums;

public enum BookingPriority
{
    Low = 1,      // Normal requests, low urgency
    Medium = 2,   // Needs attention but not urgent
    High = 3      // Urgent bookings, must handle quickly
}
